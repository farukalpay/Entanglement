# Mountain Graphics Fixture

`scene.ent` exercises the graphics runtime with reusable terrain and raster
helpers from `entlib/graphics`. It remains a fixture for parser, proof, runtime,
and image-output coverage.

```bash
entc render tests/graphics/mountain/scene.ent --mode native --width 1280 --height 720 --output assets/figures/mountain.png --json
```
